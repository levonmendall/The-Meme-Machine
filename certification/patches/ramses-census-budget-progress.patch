diff --git a/robinhood_research/ramses_extended_test.py b/robinhood_research/ramses_extended_test.py
index b17aaf36..7a02626c 100644
--- a/robinhood_research/ramses_extended_test.py
+++ b/robinhood_research/ramses_extended_test.py
@@ -84,6 +84,7 @@ CAMPAIGN_TRANSIENT_PROVIDER_BOUNDARIES=frozenset((
 SCAN_TRANSIENT_TRANSPORT_BOUNDARIES=frozenset((
     'provider_http_429','provider_rpc_429','provider_rate_limit',
 ))
+SCAN_LOCAL_BUDGET_BOUNDARIES=frozenset(('ramses_provider_program_budget_exhausted',))
 
 
 def _campaign_candidate_boundary(reason):
@@ -105,11 +106,13 @@ def _scan_with_capacity_recovery(scan_call,*,deadline,record_failure,clock=None,
             return result
         except BoundaryError as exc:
             reason=str(exc)
-            if reason in SCAN_TRANSIENT_TRANSPORT_BOUNDARIES:
+            if reason in SCAN_TRANSIENT_TRANSPORT_BOUNDARIES | SCAN_LOCAL_BUDGET_BOUNDARIES:
+                local_budget=reason in SCAN_LOCAL_BUDGET_BOUNDARIES
                 record_failure(dict(
                     attempt=attempt,reason=reason,at_monotonic=clock(),
-                    original_deadline=deadline,failure_domain='provider_transport',
-                    failed_admission_reached_transport=True,market_screen_complete=False,
+                    original_deadline=deadline,
+                    failure_domain='local_budget' if local_budget else 'provider_transport',
+                    failed_admission_reached_transport=not local_budget,market_screen_complete=False,
                     qualification_inferred=False,infrastructure_censored=True))
                 # BoundedMultiRpc has already exhausted its same-endpoint transport
                 # retries. Do not replay the whole expensive census immediately or
diff --git a/robinhood_research/ramses_universe.py b/robinhood_research/ramses_universe.py
index e8145bd3..c58bbc96 100644
--- a/robinhood_research/ramses_universe.py
+++ b/robinhood_research/ramses_universe.py
@@ -49,6 +49,7 @@ UNIVERSE_BATCH_SIZE = 8
 UNIVERSE_BATCH_PAUSE_SECONDS = 0.8
 UNIVERSE_RATE_RETRIES = 2
 UNIVERSE_RATE_COOLDOWN_SECONDS = 8.0
+UNIVERSE_LOG_BLOCK_CHUNK = 1000
 FACTORY_FETCH_CHUNK = 8
 FACTORY_SEED = Path(__file__).resolve().parents[1] / "robinhood-ramses-all-pool-inventory-cache.json"
 FACTORY_CACHE = Path(os.environ.get(
@@ -68,7 +69,7 @@ _FACTORY_INVENTORY_CACHE = {}
 _FACTORY_INVENTORY_LOCK = threading.Lock()
 
 
-def _batched_logs(rpc, start, end, addresses):
+def _batched_logs(rpc, start, end, addresses, progress=None):
     if start > end or not addresses:
         return []
     economic_topics = [
@@ -77,22 +78,31 @@ def _batched_logs(rpc, start, end, addresses):
         topic("WithdrawnFromBins(address,address,uint256[],bytes32[])"),
     ]
     calls = []
-    for first in range(start, end + 1, LOG_BLOCK_CHUNK):
+    # The public census observes ~18,000 blocks per 30 minutes. Reusing the
+    # ten-block transaction-replay page size exceeds the unchanged 1,400-call
+    # program budget before the first screen can complete. This observation-only
+    # range is capability-probed on the same endpoint; replay remains ten blocks.
+    for first in range(start, end + 1, UNIVERSE_LOG_BLOCK_CHUNK):
         calls.append((
             "eth_getLogs",
             [dict(
                 fromBlock=hex(first),
-                toBlock=hex(min(end, first + LOG_BLOCK_CHUNK - 1)),
+                toBlock=hex(min(end, first + UNIVERSE_LOG_BLOCK_CHUNK - 1)),
                 address=list(addresses),
                 topics=[economic_topics],
             )],
         ))
     found = []
+    completed=0
     for i in range(0, len(calls), 20):
         for page in rpc.batch(calls[i:i + 20], scope="universe_logs"):
             found.extend(page)
             if len(found) > MAX_SWAP_LOGS * 2:
                 raise BoundaryError("ramses_universe_economic_log_capacity")
+            completed+=1
+        if progress is not None:
+            progress(log_pages_completed=completed,log_pages_total=len(calls),
+                     log_rows_observed=len(found),log_range_start=start,log_range_end=end)
     return found
 
 
@@ -568,7 +578,8 @@ def scan(
         factory_runtime_sha256=factory_identity["runtime_sha256"],
     )
     checkpoint('economic_log_census',pools_total=len(addresses))
-    logs = _batched_logs(observation_rpc, start, end, addresses)
+    logs = _batched_logs(observation_rpc, start, end, addresses,
+                        progress=lambda **fields:checkpoint('economic_log_census',**fields))
     histories, cost_events = _decode_economic_logs(logs, addresses)
 
     # Active Wide Maker is intentionally a quiet-entry USDG strategy. Keep
diff --git a/robinhood_tests/test_ramses_universe.py b/robinhood_tests/test_ramses_universe.py
index fbefa682..a178cafb 100644
--- a/robinhood_tests/test_ramses_universe.py
+++ b/robinhood_tests/test_ramses_universe.py
@@ -336,7 +336,7 @@ class RamsesAllPoolUniverseTests(unittest.TestCase):
     def test_log_queries_cover_all_addresses_in_bounded_block_chunks(self):
         rpc = _LogRpc()
         addresses = ["0x" + "11" * 20, "0x" + "22" * 20]
-        got = ramses_universe._batched_logs(rpc, 100, 125, addresses)
+        got = ramses_universe._batched_logs(rpc, 100, 2325, addresses)
         self.assertEqual(got, [])
         self.assertEqual(len(rpc.calls), 3)
         for scope, call in rpc.calls:
@@ -346,7 +346,7 @@ class RamsesAllPoolUniverseTests(unittest.TestCase):
             self.assertEqual(query["address"], addresses)
             self.assertLessEqual(
                 int(query["toBlock"], 16) - int(query["fromBlock"], 16) + 1,
-                10,
+                1000,
             )
 
     def test_prestate_accepts_non_native_authenticated_pool(self):
diff --git a/robinhood_tests/test_ramses_census_budget_regression.py b/robinhood_tests/test_ramses_census_budget_regression.py
new file mode 100644
index 00000000..0d77ab8b
--- /dev/null
+++ b/robinhood_tests/test_ramses_census_budget_regression.py
@@ -0,0 +1,38 @@
+import unittest
+from robinhood_research import BoundaryError
+from robinhood_research.ramses_universe import _batched_logs
+from robinhood_research.ramses_extended_test import _scan_with_capacity_recovery
+
+
+class CensusBudgetRegressionTests(unittest.TestCase):
+    def test_observed_thirty_minute_window_covers_every_block_within_frozen_budget(self):
+        queries=[]; progress=[]
+        class Rpc:
+            def batch(self,calls,scope):
+                queries.extend(call[1][0] for call in calls)
+                return [[] for _ in calls]
+        # Exact window from run 35905479952, which exhausted seven 200-call sessions.
+        _batched_logs(Rpc(),70719011,70736895,['pool'],lambda **x:progress.append(x))
+        covered=[]
+        for q in queries:
+            covered.extend(range(int(q['fromBlock'],16),int(q['toBlock'],16)+1))
+            self.assertEqual(q['address'],['pool'])
+        self.assertEqual(covered,list(range(70719011,70736896)))
+        self.assertEqual(len(queries),18)
+        self.assertLess(len(queries)+22+1,7*200)
+        self.assertEqual(progress[-1]['log_pages_completed'],18)
+        self.assertEqual(progress[-1]['log_pages_total'],18)
+
+    def test_budget_exhaustion_is_censored_without_restarting_census_or_inventing_screen(self):
+        failures=[]; calls=[]
+        def scan():
+            calls.append(1)
+            raise BoundaryError('ramses_provider_program_budget_exhausted')
+        result=_scan_with_capacity_recovery(scan,deadline=100,record_failure=failures.append,
+            clock=lambda:10,sleeper=lambda _:None)
+        self.assertTrue(result['_scan_deferred'])
+        self.assertEqual(calls,[1])
+        self.assertEqual(failures[0]['failure_domain'],'local_budget')
+        self.assertFalse(failures[0]['failed_admission_reached_transport'])
+        self.assertFalse(failures[0]['market_screen_complete'])
+        self.assertTrue(failures[0]['infrastructure_censored'])
