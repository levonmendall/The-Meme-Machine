Validate DLMM acquisition v2 on mainnet: census the full API-visible eligible SOL-paired pool universe, observe finalized DLMM program activity from the union of public Solana + authenticated OnFinality WebSockets, deduplicate signatures globally, reconstruct through the adaptive authenticated-OnFinality 5-RPS HTTP queue with Alchemy rescue-only, and emit a pre-PnL wallet cohort. No strategy freeze or allocation authority.
