Verify exact-endpoint provider-role isolation: discovery and DLMM may use separately provisioned endpoints from the same vendor, but neither may reuse the authoritative primary endpoint. Preserve full observation and unchanged strategy authority.