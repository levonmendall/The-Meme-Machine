from pathlib import Path

provider=Path('meme_machine/provider.py')
text=provider.read_text()
old="""        self.calls = self.http_requests = self.failures = self.cache_hits = self.retries = 0\n        self.failure_kinds = {}\n        self.cache = {}\n"""
new="""        self.calls = self.http_requests = self.failures = self.cache_hits = self.retries = 0\n        self.batch_fallbacks = self.batch_fallback_items = self.null_retries = 0\n        self.failure_kinds = {}\n        self.failure_methods = {}\n        self.cache = {}\n"""
assert old in text
text=text.replace(old,new,1)

old="""    def call_many(self, method, params_list, priority=False, batch_size=8):\n        \"\"\"Read-only bounded JSON-RPC batching while preserving logical budgets.\n\n        Public Solana RPC can return JSON-RPC batch responses out of order, so ids\n        are verified explicitly. Every subrequest consumes the same logical request\n        budget as an individual call; batching only removes transport/latency waste.\n        Custom/test transports keep the old one-request-at-a-time behavior.\n        \"\"\"\n        if method not in self.ALLOWED:\n            raise ValueError('read-only method allowlist')\n        if not 1 <= batch_size <= 8:\n            raise ValueError('batch_size must be 1..8')\n        params_list=list(params_list)\n        if not params_list:\n            return []\n        if self.transport != self._http:\n            return [self.call(method,params,priority) for params in params_list]\n\n        results=[None]*len(params_list)\n        missing=[]\n        for index,params in enumerate(params_list):\n            params=params or []\n            key=self._key(method,params)\n            hit,value=self._cache_get(key)\n            if hit:\n                results[index]=value\n            else:\n                missing.append((index,params,key))\n\n        cap=self._cap(priority)\n        while missing:\n            chunk=missing[:batch_size]\n            missing=missing[batch_size:]\n            logical=len(chunk)\n            last_error=None\n            values=None\n            for attempt in range(2):\n                if self.calls + logical > cap:\n                    raise Unavailable('provider_budget_exhausted')\n                # Eight getTransaction items every ~2 seconds stays at a bounded\n                # four logical requests/sec while completing dense windows quickly.\n                self._pace(max(0.5,logical/4.0))\n                first_id=self.calls+1\n                requests=[{'jsonrpc':'2.0','id':first_id+i,'method':method,'params':params}\n                          for i,(_,params,_) in enumerate(chunk)]\n                self.calls += logical\n                self.http_requests += 1\n                try:\n                    response=self.transport(requests)\n                    if not isinstance(response,list):\n                        raise Unavailable('provider_error')\n                    by_id={item.get('id'):item for item in response if isinstance(item,dict)}\n                    values=[]\n                    for i in range(logical):\n                        item=by_id.get(first_id+i)\n                        if not item or item.get('error') or 'result' not in item:\n                            raise Unavailable('provider_error')\n                        values.append(item['result'])\n                    last_error=None\n                    break\n                except Exception as exc:\n                    self.failures += logical\n                    kind=self._failure_kind(exc)\n                    self.failure_kinds[kind]=self.failure_kinds.get(kind,0)+logical\n                    last_error=exc\n                    if attempt == 0:\n                        self.retries += logical\n                        self.sleep(self._retry_delay(exc))\n            if last_error is not None:\n                raise Unavailable('provider_request_failed') from None\n            for (index,_params,key),value in zip(chunk,values):\n                results[index]=value\n                self._cache_put(key,value)\n        return results\n"""
new="""    def _record_failure(self, method, kind, count=1):\n        self.failures += count\n        self.failure_kinds[kind]=self.failure_kinds.get(kind,0)+count\n        key=f'{method}:{kind}'\n        self.failure_methods[key]=self.failure_methods.get(key,0)+count\n\n    def call_many(self, method, params_list, priority=False, batch_size=8):\n        \"\"\"Read-only bounded JSON-RPC batching with fail-closed item recovery.\n\n        A batch is only a transport optimization. Successful subresponses are retained.\n        Missing/error/null `getTransaction` items are retried individually so one public\n        RPC batch anomaly cannot discard an otherwise complete finalized interval. The\n        fallback consumes the normal logical request budget and never widens evidence\n        scope, transaction count, commitment, or freshness bounds.\n        \"\"\"\n        if method not in self.ALLOWED:\n            raise ValueError('read-only method allowlist')\n        if not 1 <= batch_size <= 8:\n            raise ValueError('batch_size must be 1..8')\n        params_list=list(params_list)\n        if not params_list:\n            return []\n        if self.transport != self._http:\n            return [self.call(method,params,priority) for params in params_list]\n\n        results=[None]*len(params_list)\n        missing=[]\n        for index,params in enumerate(params_list):\n            params=params or []\n            key=self._key(method,params)\n            hit,value=self._cache_get(key)\n            if hit:\n                results[index]=value\n            else:\n                missing.append((index,params,key))\n\n        cap=self._cap(priority)\n        while missing:\n            chunk=missing[:batch_size]\n            missing=missing[batch_size:]\n            logical=len(chunk)\n            if self.calls + logical > cap:\n                raise Unavailable('provider_budget_exhausted')\n            self._pace(max(0.5,logical/4.0))\n            first_id=self.calls+1\n            requests=[{'jsonrpc':'2.0','id':first_id+i,'method':method,'params':params}\n                      for i,(_,params,_) in enumerate(chunk)]\n            self.calls += logical\n            self.http_requests += 1\n            fallback=[]\n            try:\n                response=self.transport(requests)\n                if not isinstance(response,list):\n                    raise Unavailable('provider_error')\n                by_id={}\n                for item in response:\n                    if not isinstance(item,dict) or item.get('id') in by_id:\n                        continue\n                    by_id[item.get('id')]=item\n                for i,(index,params,key) in enumerate(chunk):\n                    item=by_id.get(first_id+i)\n                    if not item or item.get('error') or 'result' not in item:\n                        self._record_failure(method,'provider_error')\n                        fallback.append((index,params,key))\n                        continue\n                    value=item['result']\n                    if method=='getTransaction' and value is None:\n                        self._record_failure(method,'null_result')\n                        fallback.append((index,params,key))\n                        continue\n                    results[index]=value\n                    self._cache_put(key,value)\n            except Exception as exc:\n                kind=self._failure_kind(exc)\n                self._record_failure(method,kind,logical)\n                fallback=list(chunk)\n            if fallback:\n                self.batch_fallbacks += 1\n                self.batch_fallback_items += len(fallback)\n                for index,params,key in fallback:\n                    # Individual calls retain finalized commitment and retry once on\n                    # the real HTTP path. This is intentionally narrower than\n                    # repeating an identical failing batch.\n                    value=self.call(method,params,priority)\n                    if method=='getTransaction' and value is None:\n                        # A finalized signature with a still-null body is incomplete\n                        # evidence. Never convert it into an empty/benign mutation.\n                        raise Unavailable('provider_transaction_unavailable')\n                    results[index]=value\n                    self._cache_put(key,value)\n        return results\n"""
assert old in text
text=text.replace(old,new,1)
provider.write_text(text)

# Record method-level failures from ordinary single calls too, and give a null
# getTransaction one bounded retry before accepting it as unavailable evidence.
text=provider.read_text()
old="""                if response.get('error') or 'result' not in response:\n                    raise Unavailable('provider_error')\n                result = response['result']\n                last_error = None\n                break\n            except Exception as exc:\n                self.failures += 1\n                kind=self._failure_kind(exc)\n                self.failure_kinds[kind]=self.failure_kinds.get(kind,0)+1\n                last_error = exc\n                if attempt+1 < attempts:\n                    self.retries += 1\n                    self.sleep(self._retry_delay(exc))\n"""
new="""                if response.get('error') or 'result' not in response:\n                    raise Unavailable('provider_error')\n                result = response['result']\n                if method=='getTransaction' and result is None and attempt+1 < attempts:\n                    self._record_failure(method,'null_result')\n                    self.null_retries += 1\n                    self.retries += 1\n                    self.sleep(0.5)\n                    continue\n                last_error = None\n                break\n            except Exception as exc:\n                kind=self._failure_kind(exc)\n                self._record_failure(method,kind)\n                last_error = exc\n                if attempt+1 < attempts:\n                    self.retries += 1\n                    self.sleep(self._retry_delay(exc))\n"""
assert old in text
provider.write_text(text.replace(old,new,1))

research=Path('tests/dlmm_strategy_high_activity.py')
text=research.read_text()
old='''    rpc = PoolScanRPC("https://api.mainnet-beta.solana.com", limit=240)\n'''
new='''    rpc_url = os.environ.get("MM_SOLANA_READ_RPC_URL") or "https://api.mainnet.solana.com"\n    rpc = PoolScanRPC(rpc_url, limit=240)\n'''
assert old in text
text=text.replace(old,new,1)
old='''        provider_failure_kinds=rpc.failure_kinds,\n    )\n'''
new='''        provider_failure_kinds=rpc.failure_kinds,\n        provider_failure_methods=rpc.failure_methods,\n        rpc_batch_fallbacks=rpc.batch_fallbacks,\n        rpc_batch_fallback_items=rpc.batch_fallback_items,\n        rpc_null_retries=rpc.null_retries,\n    )\n'''
assert old in text
research.write_text(text.replace(old,new,1))

tests=Path('tests/test_provider_retry.py')
text=tests.read_text()
insert='''\n    def test_batch_item_error_falls_back_only_for_failed_item(self):\n        class PartialBatchRPC(RPC):\n            def __init__(self):\n                self.single=[]\n                super().__init__('https://example.invalid',limit=40,clock=lambda:100.0,sleeper=lambda _seconds:None)\n            def _http(self,request):\n                if isinstance(request,list):\n                    rows=[]\n                    for i,item in enumerate(request):\n                        if i==1:\n                            rows.append({'jsonrpc':'2.0','id':item['id'],'error':{'code':-32005}})\n                        else:\n                            rows.append({'jsonrpc':'2.0','id':item['id'],'result':{'signature':item['params'][0]}})\n                    return rows\n                self.single.append(request['params'][0])\n                return {'jsonrpc':'2.0','id':request['id'],'result':{'signature':request['params'][0]}}\n        rpc=PartialBatchRPC()\n        params=[[f'sig-{i}',{'encoding':'json','commitment':'finalized','maxSupportedTransactionVersion':0}] for i in range(4)]\n        out=rpc.call_many('getTransaction',params,True,batch_size=4)\n        self.assertEqual([x['signature'] for x in out],[f'sig-{i}' for i in range(4)])\n        self.assertEqual(rpc.single,['sig-1'])\n        self.assertEqual((rpc.batch_fallbacks,rpc.batch_fallback_items),(1,1))\n        self.assertEqual(rpc.failure_methods,{'getTransaction:provider_error':1})\n        self.assertEqual((rpc.calls,rpc.http_requests),(5,2))\n\n    def test_rejected_batch_degrades_to_bounded_individual_reads(self):\n        class NoBatchRPC(RPC):\n            def __init__(self):\n                self.singles=0\n                super().__init__('https://example.invalid',limit=40,clock=lambda:100.0,sleeper=lambda _seconds:None)\n            def _http(self,request):\n                if isinstance(request,list):\n                    return {'jsonrpc':'2.0','id':1,'error':{'code':-32600}}\n                self.singles+=1\n                return {'jsonrpc':'2.0','id':request['id'],'result':{'signature':request['params'][0]}}\n        rpc=NoBatchRPC()\n        params=[[f'sig-{i}',{'encoding':'json','commitment':'finalized','maxSupportedTransactionVersion':0}] for i in range(4)]\n        out=rpc.call_many('getTransaction',params,True,batch_size=4)\n        self.assertEqual(len(out),4);self.assertEqual(rpc.singles,4)\n        self.assertEqual((rpc.batch_fallbacks,rpc.batch_fallback_items),(1,4))\n        self.assertEqual(rpc.failure_methods,{'getTransaction:provider_error':4})\n        self.assertEqual((rpc.calls,rpc.http_requests),(8,5))\n\n    def test_individual_transaction_null_retries_once(self):\n        class NullOnceRPC(RPC):\n            def __init__(self):\n                self.n=0\n                super().__init__('https://example.invalid',limit=40,clock=lambda:100.0,sleeper=lambda _seconds:None)\n            def _http(self,request):\n                self.n+=1\n                return {'jsonrpc':'2.0','id':request['id'],'result':None if self.n==1 else {'slot':1}}\n        rpc=NullOnceRPC()\n        value=rpc.call('getTransaction',['sig',{'encoding':'json','commitment':'finalized','maxSupportedTransactionVersion':0}],True)\n        self.assertEqual(value,{'slot':1})\n        self.assertEqual((rpc.null_retries,rpc.retries,rpc.failures),(1,1,1))\n        self.assertEqual(rpc.failure_methods,{'getTransaction:null_result':1})\n'''
marker='''\n    def test_unknown_signature_time_cannot_claim_complete_window(self):\n'''
assert marker in text
tests.write_text(text.replace(marker,insert+marker,1))

workflow=Path('.github/workflows/dlmm-strategy-research.yml')
text=workflow.read_text()
old='''      - name: Highest-activity finalized DLMM strategy replay\n        run: python -m tests.dlmm_strategy_high_activity_stonk --cycles 1 --window-seconds 12\n'''
new='''      - name: Highest-activity finalized DLMM strategy replay\n        env:\n          MM_SOLANA_READ_RPC_URL: ${{ secrets.MM_SOLANA_READ_RPC_URL }}\n        run: python -m tests.dlmm_strategy_high_activity_stonk --cycles 1 --window-seconds 12\n'''
assert old in text
workflow.write_text(text.replace(old,new,1))

status=Path('DLMM_RESEARCH_STATUS.md')
if status.exists():
    text=status.read_text()
    text += '''\n## Read-only RPC acquisition repair\n\n- Finalized transaction batches are transport optimization only; a partial/rejected batch now falls back only for unresolved items.\n- `getTransaction` null is retried once and remains fail-closed if still unavailable.\n- Research defaults to Solana's canonical public mainnet endpoint and accepts optional `MM_SOLANA_READ_RPC_URL` for an authorized dedicated read-only endpoint.\n- No transaction count, finality, freshness, strategy, cost, or allocation boundary is widened.\n'''
    status.write_text(text)

# Remove one-time repair plumbing from the resulting repair commit.
Path('.github/scripts/repair_dlmm_rpc_acquisition.py').unlink(missing_ok=True)
Path('.github/workflows/dlmm-rpc-acquisition-repair.yml').unlink(missing_ok=True)
