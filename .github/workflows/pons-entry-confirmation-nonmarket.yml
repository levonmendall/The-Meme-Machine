name: Pons entry confirmation non-market certification
on:
  push:
    branches: [repair/pons-entry-confirmation-ordering-20260925]
permissions:
  contents: read
  actions: read
jobs:
  certify:
    if: contains(github.event.head_commit.message, '[non-market-cert]')
    uses: ./.github/workflows/non-market-certification.yml
    secrets: inherit
