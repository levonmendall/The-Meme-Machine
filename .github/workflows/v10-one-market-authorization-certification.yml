name: v10-one-market-authorization-certification
on:
  push:
    branches: [cert/v10-one-market-authorization-20260924]

permissions:
  contents: read
  actions: read

jobs:
  certify:
    if: contains(github.event.head_commit.message, '[non-market-cert]')
    uses: ./.github/workflows/non-market-certification.yml
    secrets: inherit
