name: v9-handoff-continuation-nonmarket-certification
on:
  push:
    branches: [repair/v9-meteora-handoff-position-continuation-20260924]
permissions:
  contents: read
  actions: read
concurrency:
  group: v9-handoff-continuation-exact-certificate
  cancel-in-progress: false
jobs:
  certify:
    if: contains(github.event.head_commit.message, '[v9-handoff-cert]') && contains(github.event.head_commit.message, '[non-market-cert]')
    uses: ./.github/workflows/non-market-certification.yml
    secrets: inherit
# Non-market only. No smoke, hourly campaign, successor, or position continuation is dispatched here.
