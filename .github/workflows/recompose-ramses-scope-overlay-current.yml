name: recompose-ramses-scope-overlay-current
on:
  push:
    branches: [repair/market-scope-efficiency-current-v2-20260923]
permissions:
  contents: write
jobs:
  recompose:
    if: contains(github.event.head_commit.message, '[recompose-ramses-scope]')
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          fetch-depth: 0
      - name: Recompose scope overlay on prepared current Ramses base
        run: |
          set -euo pipefail
          SHA="$(python -c 'import json; print(json.load(open("certification/sources.json"))["lanes"]["ramses"]["source_sha"])')"
          WT="$RUNNER_TEMP/ramses"
          git fetch origin "$SHA"
          git worktree add --detach "$WT" "$SHA"
          git -C "$WT" apply --check "$GITHUB_WORKSPACE/certification/patches/ramses-admission.patch"
          git -C "$WT" apply --index "$GITHUB_WORKSPACE/certification/patches/ramses-admission.patch"
          git -C "$WT" config user.name "recompose"
          git -C "$WT" config user.email "recompose@example.invalid"
          git -C "$WT" commit -m "temporary prepared Ramses base"
          git -C "$WT" apply --3way --index "$GITHUB_WORKSPACE/certification/patches/market-scope-efficiency-ramses.patch"
          git -C "$WT" diff --check --cached
          git -C "$WT" diff --binary --cached HEAD > "$GITHUB_WORKSPACE/certification/patches/market-scope-efficiency-ramses.patch"
      - name: Commit recomposed scope overlay
        run: |
          if git diff --quiet -- certification/patches/market-scope-efficiency-ramses.patch; then
            echo "No scope overlay rewrite required"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add certification/patches/market-scope-efficiency-ramses.patch
          git commit -m "[qualification-build] Recompose Ramses market-scope overlay on current base"
          git push origin HEAD:repair/market-scope-efficiency-current-v2-20260923
