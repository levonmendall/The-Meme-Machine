name: Runtime v2 promotion non-market certification
on:
  push:
    branches: [cert/single-market-runtime-v2-20260926]
permissions:
  contents: read
  actions: read
jobs:
  certify:
    if: contains(github.event.head_commit.message, '[non-market-cert]')
    uses: ./.github/workflows/non-market-certification.yml
    secrets: inherit
