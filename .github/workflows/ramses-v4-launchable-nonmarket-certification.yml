name: Ramses v4 launchable non-market certification
on:
  push:
    branches: [strategy/ramses-viability-v4-20260924]

permissions:
  contents: read
  actions: read

jobs:
  certify:
    if: contains(github.event.head_commit.message, '[non-market-cert]')
    uses: ./.github/workflows/non-market-certification.yml
    secrets: inherit
