name: counterfactual-strategy-v1-certification
on:
  push:
    branches: [strategy/counterfactual-replay-v1-20260924]
permissions:
  contents: read
  actions: read
jobs:
  certify:
    if: contains(github.event.head_commit.message, '[non-market-cert]')
    uses: ./.github/workflows/non-market-certification.yml
    secrets: inherit
