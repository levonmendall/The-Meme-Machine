name: v9-launch-guard-nonmarket-certification
on:
  push:
    branches: [repair/v9-launch-guard-reauthorization-20260924]
permissions:
  contents: read
  actions: read
concurrency:
  group: v9-launch-guard-exact-certificate
  cancel-in-progress: false
jobs:
  certify:
    if: contains(github.event.head_commit.message, '[v9-guard-cert]') && contains(github.event.head_commit.message, '[non-market-cert]')
    uses: ./.github/workflows/non-market-certification.yml
    secrets: inherit
# Non-market only. No smoke, hourly campaign, successor, or continuation dispatch.
