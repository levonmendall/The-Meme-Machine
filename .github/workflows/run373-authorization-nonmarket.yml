name: Run 373 authorization non-market certification
on:
  push:
    branches: [cert/single-market-run373-run372-large-frame-repair-20260926]
permissions:
  contents: read
  actions: read
jobs:
  certify:
    if: contains(github.event.head_commit.message, '[non-market-cert]')
    uses: ./.github/workflows/non-market-certification.yml
    secrets: inherit
# Authorization-only certification. No market dispatch is present in this workflow.
